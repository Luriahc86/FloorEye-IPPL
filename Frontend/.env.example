# FloorEye Frontend Environment Variables
# Copy this file to .env.local for development

# Backend API Base URL (Railway production)
# ⚠️ IMPORTANT: MUST use HTTPS (not HTTP) to avoid Mixed Content errors!
# ⚠️ MUST NOT have a trailing slash!
# ⚠️ Variable name is VITE_API_BASE (not VITE_API_BASE_URL)
#